# RAG-pipeline
A Retrieval-Augmented Generation (RAG) pipeline enhances large language models by grounding their outputs in external knowledge. Instead of relying solely on the model’s parameters, RAG retrieves relevant information from a knowledge base and injects it into the prompt before generation.
